#include<stdlib.h>
void main(){
    char str[]="Hello World";
    int i,len;
    len=strlen(str);
    for(i=0;i<len;i++){
        str1[i]=str[i]^0;
    }
    for(i=0;i<len;i++){
        printf("%c",str1[i]);
    }
   getch();
}
